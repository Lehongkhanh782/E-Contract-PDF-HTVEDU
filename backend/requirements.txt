fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-multipart==0.0.20
itsdangerous==2.2.0
pillow==11.1.0
# Chỉ dùng để ký khóa tài khoản máy; gọi Google Sheets bằng httpx cho nhẹ.
google-auth==2.38.0
# google-auth bắt buộc phải có requests mới xin được vé vào cửa: lớp
# Request của nó nằm trong google.auth.transport.requests và sẽ báo
# ImportError nếu thiếu. Không thể bỏ để dùng mỗi httpx.
requests==2.32.3
httpx==0.28.1

# Các thư viện của phần lõi contract_kit
lxml==6.1.1
pypdf==6.10.0
reportlab==4.4.9
